import {icon,link} from './ui.js';
export const chapters=[['A little extra.','A lot of possibility.','The hostel kitchen packs its surplus into ready-to-collect boxes. Ingredients, portions, and a pickup window make the next step clear.'],['Reserved for care.','Room for more.','An approved NGO reserves the quantity it can collect. Remaining boxes stay available for other organizations. Every reservation has its own pickup details.'],['A short journey.','A meaningful arrival.','The NGO arranges transport and heads to the kitchen. A pickup contact and arrival time keep everyone on the same page.'],['One code.','A confirmed collection.','The hostel checks the pickup code and records the actual collected boxes. Reservation and collection are tracked separately, right through the last handover.']];
export const fallback=()=>`<div class="static-scene"><div class="static-boxes" aria-hidden="true"><i></i><i></i><i></i></div><div><strong>Hostel kitchen</strong><span>List → Reserve → Collect</span><strong>Community table</strong></div></div>`;
const scene=hero=>`<div class="scene-container" data-scene="${hero?'hero':'journey'}" role="img" aria-label="Illustrative 3D hostel, food boxes, pickup van and community table; not a model of the GLA campus">${fallback()}</div>`;
export function home(cards){return `
<section class="hero"><div class="hero-copy"><span class="eyebrow">GLA UNIVERSITY · MATHURA</span><h1>Good food.<br>Better routes.<br><em>Shared futures.</em></h1><p>A direct connection between GLA hostel kitchens and verified NGOs. Share surplus meals. Coordinate the pickup. Close the loop.</p><div class="actions">${link('/login?role=hostel','Donate Food '+icon('arrow'))}${link('/explore','Find Available Food','button outline')}</div><div class="hero-note">${icon('leaf',16)} A little less waste. A lot more possibility.</div></div><div class="hero-art">${scene(true)}${Array.from({length:9},(_,i)=>`<i aria-hidden="true" class="seed" style="left:${12+i*9}%;top:${10+(i*23)%70}%;animation-delay:${i*.7}s"></i>`).join('')}<span class="scene-caption">A CONNECTED FOOD-SHARING NETWORK</span><a href="/#how" data-link class="hero-scroll">${icon('down',17)} Scroll to follow the food</a></div></section>
<div class="principles"><span>${icon('leaf',18)} Good food, shared locally</span><span>Always free to collect</span><span>Hostel kitchens → community tables</span></div>
<section id="how" class="journey"><div class="journey-sticky"><div class="journey-top"><span>Follow a meal’s next chapter</span><span>Scroll to move the story ↓</span></div><div class="journey-scene">${scene(false)}</div><div class="journey-copy"><span class="chapter-index">01 / 04</span><h2>${chapters[0][0]}<br><em>${chapters[0][1]}</em></h2><p>${chapters[0][2]}</p></div><div class="journey-ledger"><span>THE HANDOVER FLOW</span><div><strong data-count="available">List</strong><small>kitchen</small></div><div><strong data-count="reserved">Book</strong><small>NGO</small></div><div><strong data-count="collected">Collect</strong><small>together</small></div></div><div class="journey-track"><div class="track-fill"></div>${['Pack & list','Reserve boxes','Make the journey','Confirm collection'].map((t,i)=>`<span class="${i===0?'current':''}"><b>${i+1}</b>${t}</span>`).join('')}</div><div class="mobile-chapters">${chapters.map(([a,b,p],i)=>`<article><span>0${i+1}</span><h3>${a} ${b}</h3><p>${p}</p></article>`).join('')}</div></div></section>
<section class="manifesto section"><div class="manifesto-heading">${icon('leaf',36)}<h2>A meal’s story<br>shouldn’t end<br><em>in a bin.</em></h2></div><div class="manifesto-copy"><p class="big-copy">The lunch rush ends.<br>The opportunity doesn’t.</p><p>Hostel kitchens cook for a community every day. When there’s more than needed, MealBridge creates a clear path from that kitchen to an NGO ready to collect it.</p><p>It’s a simple connection with a practical rhythm: make the food visible, reserve the right quantity, and coordinate the handover.</p>${link('/explore','Find the next destination '+icon('arrow',18),'text-link')}</div></section>
<section class="pickup-guide section"><div class="section-heading"><h2>A thoughtful pickup<br>starts before the door.</h2><p>A few clear details make the handover easier for everyone.</p></div><div class="guide-layout"><div class="guide-image"><img src="/assets/meal-tray.png" alt="Illustrative meal packed into a compartment tray" loading="lazy"><span>Illustrative meal photograph · not a food listing</span></div><div class="guide-list">${[['box','Know what’s in the box','Read the ingredients, known allergens, portions and packaging details. Ask the kitchen about anything that is unclear.'],['clock','Plan around the pickup window','Reserve only what your team can collect before the deadline. Add an expected arrival time and a reachable pickup contact.'],['truck','Bring the right transport','NGOs arrange transportation. Check whether crates or containers are needed, and follow the kitchen’s storage and handling instructions.'],['check','Close the loop together','Share the pickup code at the kitchen. The hostel records the actual collected quantity, so the dashboard reflects the handover.']].map(([i,t,p])=>`<article>${icon(i,24)}<div><h3>${t}</h3><p>${p}</p></div></article>`).join('')}</div></div></section>
<section class="shared-promise"><div><h2>Clear by design.<br>Human at heart.</h2><p>Food sharing works best when every participant knows what happens next.</p></div><div class="promise-list">${['Food donations are always free.','NGOs choose the quantity they can collect.','Pickup details follow the reservation.','Cancellations and collections stay visible.'].map(t=>`<p>${icon('check')}${t}</p>`).join('')}</div></section>
<section class="section available-section"><div class="section-heading"><div><h2>Fresh opportunities<br>to make a difference.</h2><p>Food published by approved hostel representatives.</p></div>${link('/explore','Explore all food '+icon('arrow'),'button outline')}</div><div class="food-grid">${cards||'<p>No available meals to show. Approved kitchens can publish food when it is ready to share.</p>'}</div></section>
<section class="participation"><div><h2>Two sides.<br>One shared table.</h2><p>It starts with a kitchen that has a little extra, and a community ready to bring it somewhere meaningful.</p>${link('/organizations','Meet the community '+icon('arrow'),'text-link')}</div><div class="role-invite">${icon('box',30)}<h3>For hostels</h3><p>Give surplus meals a purpose. List boxes, coordinate pickups, and see what was collected.</p>${link('/login?role=hostel','Start sharing '+icon('arrow'),'button lime')}</div><div class="role-invite">${icon('truck',30)}<h3>For NGOs</h3><p>Find food for your community. Reserve the boxes you need and arrange your pickup.</p>${link('/login?role=ngo','Find your next pickup '+icon('arrow'),'button light-outline')}</div></section>
<section class="section faq"><h2>A few things<br>worth knowing.</h2><div>${[['Is there a cost to collect food?','No. Food donations are free. NGOs arrange and cover their own transportation.'],['Can more than one NGO reserve a listing?','Yes. Each NGO reserves its own quantity. If 20 of 50 boxes are reserved, 30 remain for other NGOs.'],['What if a pickup needs to be cancelled?','Cancel from your NGO dashboard. Boxes become available again only while the listing is open and before the pickup deadline.'],['How is a pickup confirmed?','The NGO shares its pickup code. The hostel enters the code and actual collected quantity to finalize the pickup.'],['Can I change my portal later?','No. Your Hostel or NGO portal is selected once after Google sign-in and stays linked to that account. An administrator reviews your organization before participation.']].map(([q,a])=>`<details><summary>${q}<span aria-hidden="true">+</span></summary><p>${a}</p></details>`).join('')}</div></section>
<section class="closing"><div class="particle-text"><h2>Share food.<br class="mobile-only"> Spread care.</h2><canvas aria-hidden="true"></canvas></div><p>Good things happen when we make room for each other.</p>${link('/login','Join the table '+icon('arrow'),'button lime')}</section>`;}
