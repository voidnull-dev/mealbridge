import {escape as e,icon,link,field,options,roleName} from './ui.js';
import {glaHostels,hostelSource} from './campus.js';
import {stats,date,localInput} from './service.js';
const empty=(title,body)=>`<div class="empty"><h2>${title}</h2><p>${body}</p></div>`;
const signed=s=>s.user&&s.profile?.status==='approved';
export function card(s,l){const q=stats(s,l);return `<a data-link href="/food/${e(l.id)}" class="food-card"><div class="food-card-body"><span class="badge">${e(l.type)}</span><small>${e(l.hostel)} · Mathura</small><h3>${e(l.name)}</h3><p>${e(l.description)}</p><div class="food-info"><strong>${q.available} boxes</strong><span>${l.portions} portions / box</span></div><p>${icon('clock',16)} ${date(l.deadline)}</p><div class="card-bottom"><span>${q.status}</span>${icon('arrow')}</div></div></a>`;}
export function explore(s){return `<div class="page"><div class="page-heading"><div><span class="eyebrow">GLA UNIVERSITY · MATHURA</span><h1>Food with a next stop.</h1><p>Actual listings from approved hostel representatives. Reserve only what your NGO can collect.</p></div>${icon('box',48)}</div><form id="filters" class="filters" data-form="filters"><label class="search">Search food or hostel<input name="query" placeholder="Search meals, hostel or pickup area"></label><label>Food type<select name="type"><option value="">All types</option>${options(['Vegetarian','Vegan','Non-vegetarian'])}</select></label>${field('minimum','Minimum boxes','','number','min="0" step="1" placeholder="Any"')}<label>Pickup within<select name="hours"><option value="">Any time</option><option value="2">2 hours</option><option value="4">4 hours</option></select></label></form><div class="results-bar"><span id="results-count"></span><label class="check"><input type="checkbox" name="unavailable" form="filters">Include closed listings</label></div><div id="food-results"></div></div>`;}
export function login(s){return `<div class="login-page"><div class="login-copy"><span class="eyebrow">ONE ACCOUNT. ONE PURPOSE.</span><h1>Your next meal.<br><em>Someone’s next<br>opportunity.</em></h1><p>Connect your Google account, choose your portal once, and register your organization.</p><ol class="auth-steps"><li>Google sign-in</li><li>Permanent portal selection</li><li>Administrator verification</li></ol></div><section class="login-panel">${s.loading?'<h2>Checking your account…</h2>':!s.user?`<span class="eyebrow">WELCOME TO MEALBRIDGE</span><h2>Good food connects us.</h2><p>Use your Google account to continue.</p><button class="button full google-button" data-action="google"><span class="google-g" aria-hidden="true">G</span> Continue with Google</button><p class="auth-fine">No separate password. After signing in, choose Hostel or NGO. Your choice is permanently linked to this Google account.</p><div class="auth-rule"><strong>Built for verified participation.</strong><p>Your organization must be approved before it can publish or reserve food.</p></div>`:s.profile?`<span class="badge">${roleName(s.profile.role)} portal · Locked</span><h2>${e(s.profile.name)}</h2><p>Signed in as ${e(s.user.email)}.</p><p>Your portal cannot be switched with this Google account.</p>${link('/dashboard','Open your portal '+icon('arrow'))}<button class="text-button" data-action="logout">Sign out</button>`:s.admin?`<h2>Administrator account</h2><p>Review organization requests and food concerns.</p>${link('/admin','Open administration')}<button class="text-button" data-action="logout">Sign out</button>`:onboard(s)}</section></div>`;}
function onboard(s){return `<span class="eyebrow">STEP 02 · PERMANENT PORTAL</span><h2>Choose your participation portal.</h2><p>Signed in as <strong>${e(s.user.email)}</strong>. This Google account can be linked to only one portal.</p><form data-form="onboard"><fieldset class="portal-options"><legend>Select once — this cannot be changed later.</legend><label><input type="radio" name="role" value="hostel" required><strong>GLA Hostel</strong><span>For an authorized representative of a GLA University hostel/mess.</span></label><label><input type="radio" name="role" value="ngo" required><strong>NGO</strong><span>For an authorized representative of a real NGO that will collect surplus food.</span></label></fieldset><div id="hostel-registration" class="registration-section" hidden><h3>GLA hostel verification</h3><p class="form-help">Choose only a hostel documented by GLA University. This does not mean the hostel is already a MealBridge partner.</p><label>GLA hostel<select name="hostelId"><option value="">Select your hostel</option>${glaHostels.map(h=>`<option value="${e(h.id)}" data-name="${e(h.name)}" data-gender="${e(h.gender)}">${e(h.name)} · ${e(h.gender)}</option>`).join('')}</select></label><input type="hidden" name="hostelGender"><input type="hidden" name="name"><label>Authorized representative / contact<textarea name="contact" minlength="7" maxlength="200" placeholder="Name, official role and reachable phone number"></textarea></label><label>Authorization details<textarea name="evidence" minlength="20" maxlength="1500" placeholder="Explain your hostel/mess responsibility and how the administrator can verify your authorization. Do not upload Aadhaar, passwords or other sensitive identity documents."></textarea></label></div><div id="ngo-registration" class="registration-section" hidden><h3>NGO verification</h3><p class="form-help">MealBridge does not create or seed NGOs. Enter the real organization you represent; approval requires administrator verification.</p>${field('name','Registered NGO name','','text','maxlength="160" placeholder="Exact legal / registered name"')} ${field('registrationNumber','NGO registration number','','text','maxlength="120" placeholder="Registration / trust / society / Section 8 identifier"')} ${field('website','Official NGO website','','url','maxlength="300" placeholder="https://example.org"')} ${field('serviceArea','Service area in/around Mathura','','text','maxlength="300" placeholder="Where your organization serves communities"')}<label>Authorized representative / contact<textarea name="contact" minlength="7" maxlength="200" placeholder="Name, official role and reachable phone number"></textarea></label><label>Verification details<textarea name="evidence" minlength="20" maxlength="1500" placeholder="Describe the registration, public contact details and service work the administrator can independently verify."></textarea></label></div><label class="check"><input type="checkbox" name="authorization" required> I am authorized to represent this organization, the information is accurate, and I understand that my portal choice is permanent.</label><button class="button full">Lock portal & request approval</button></form><p class="auth-fine">No organization is approved automatically. Google sign-in proves the account, not the organization. An administrator must verify the hostel authorization or NGO registration before the portal becomes active.</p><button class="text-button" data-action="logout">Sign out</button>`;}
export function organizations(s){return `<div class="page"><span class="eyebrow">THE LOCAL NETWORK</span><h1>Real kitchens.<br>Real connections.</h1><p class="lead">Only approved organizations appear here. A Google account alone does not verify a hostel or NGO.</p><section class="campus-note"><div><span class="eyebrow">CAMPUS CONTEXT</span><h2>GLA University, Mathura</h2><p>GLA describes separate boys’ and girls’ hostel facilities, with wardens and hostel messes. MealBridge is an independent initiative; university participation must be confirmed.</p><a href="https://www.gla.ac.in/campus-life/hostel" target="_blank" rel="noopener">Read GLA’s official hostel information ↗</a></div><p>17km Stone, NH-19<br>Delhi–Mathura Road<br>Chaumuhan, Mathura, Uttar Pradesh</p></section><details class="hostel-reference"><summary>GLA hostel name reference</summary><p>Names documented in GLA’s 2022 self-study report. Current names and representative authorization need confirmation. This is not a list of MealBridge partners.</p><div class="table-wrap"><table><thead><tr><th>Hostel</th><th>Block</th><th>Facility</th></tr></thead><tbody>${glaHostels.map(([block,name,group])=>`<tr><td>${e(name)}</td><td>${e(block)||'—'}</td><td>${e(group)}</td></tr>`).join('')}</tbody></table></div><a href="${hostelSource}" target="_blank" rel="noopener">GLA self-study report, pages 66–67 ↗</a></details><h2>Approved organizations</h2><div class="org-list">${s.organizations.map(o=>`<article>${icon(o.role==='hostel'?'box':'leaf',28)}<div><h3>${e(o.name)}</h3><p>${roleName(o.role)} · ${e(o.city)}</p></div><span class="badge">Approved by MealBridge</span></article>`).join('')||empty('No organizations to show yet.','Registered organizations appear here after administrator approval.')}</div>${link('/login','Register your organization '+icon('arrow'))}</div>`;}
export function listingDetails(s,id){const l=s.listings.find(l=>l.id===id);if(!l)return missing();const q=stats(s,l);return `<div class="page">${link('/explore','← Available food','back')}<div class="detail-grid"><article><span class="badge">${e(l.type)}</span><h1>${e(l.name)}</h1><p class="lead">${e(l.hostel)}</p><p>${e(l.area)}, Mathura</p><h2>Know your meal.</h2><p>${e(l.description)}</p><dl class="details">${[['Prepared',date(l.prepared)],['Pickup deadline',date(l.deadline)],['Portions per box',l.portions],['Ingredients',l.ingredients],['Known allergens',l.allergens],['Storage',l.storage],['Packaging',l.packaging]].map(([k,v])=>`<div><dt>${k}</dt><dd>${e(v)}</dd></div>`).join('')}</dl>${s.user?`<details><summary>Report a concern</summary><form data-form="report" data-id="${e(id)}"><label>What needs review?<textarea name="reason" required minlength="10" maxlength="1500"></textarea></label><button class="button small">Send to administrator</button></form></details>`:''}</article><aside class="reservation-panel"><div class="availability"><strong>${q.available}</strong><span>boxes available</span></div><p>Free donation · NGOs arrange transportation.</p><hr>${!q.available?'<p>This listing cannot accept new reservations.</p>':signed(s)&&s.profile.role==='ngo'?`<form data-form="reserve" data-id="${e(id)}">${field('quantity','Boxes to collect',1,'number',`required min="1" max="${q.available}" step="1"`)}${field('eta','Expected arrival',localInput(Date.now()+1800000),'datetime-local','required')}<small>Input timezone: ${e(Intl.DateTimeFormat().resolvedOptions().timeZone)}. Deadlines display in IST.</small>${field('contact','Pickup person and phone','','text','required minlength="7" maxlength="300"')}<button class="button full">Reserve boxes</button></form>`:`<p>Reservations require an approved NGO account.</p>${link(s.user?'/dashboard':'/login',s.user?'Go to your portal':'Continue with Google','button full')}`}<p class="privacy">Pickup instructions and the collection code appear in your dashboard after reservation.</p></aside></div></div>`;}
export function dashboard(s){if(!s.user||!s.profile)return login(s);const p=s.profile;if(p.status!=='approved')return `<div class="page approval-page"><span class="eyebrow">${roleName(p.role)} PORTAL · PERMANENT</span><h1>${p.status==='pending'?'Your request is in.':'Your request needs attention.'}</h1><p class="lead">${e(p.name)}</p><div class="notice"><h2>${p.status==='pending'?'Awaiting administrator approval':'Registration not approved'}</h2><p>${e(p.reviewNote||'The administrator will verify your organization and representative details. Publishing and reservations become available only after approval.')}</p></div><dl class="details"><div><dt>Account</dt><dd>${e(s.user.email)}</dd></div><div><dt>Portal</dt><dd>${roleName(p.role)} — locked to this account</dd></div></dl><button class="button outline" data-action="refresh">Check status</button><button class="text-button" data-action="logout">Sign out</button></div>`;
 const hostel=p.role==='hostel',listings=s.listings.filter(l=>l.hostelUid===s.user.uid),rs=hostel?s.hostelReservations:s.ngoReservations;
 return `<div class="page dashboard"><div class="page-heading"><div><span class="eyebrow">${roleName(p.role)} PORTAL · LOCKED</span><h1>${hostel?'Your kitchen, connected.':'Your community pickups.'}</h1><p>${e(p.name)} · ${e(s.user.email)}</p></div>${link(hostel?'/listings/new':'/explore',hostel?'Publish food +':'Find food ↗')}</div><div class="metrics">${[['Upcoming pickups',rs.filter(r=>r.status==='reserved'&&r.deadline>Date.now()).length],['Boxes collected',rs.reduce((a,r)=>a+r.collected,0)]].map(([a,b])=>`<div><strong>${b}</strong><span>${a}</span></div>`).join('')}</div>${hostel?`<h2>Your listings</h2>${listings.map(l=>`<article class="pickup-row"><div><h3>${e(l.name)}</h3><p>${stats(s,l).available} available · ${l.reserved} reserved · ${l.collected} collected</p><small>${date(l.deadline)} · ${stats(s,l).status}</small></div><div class="actions">${link('/food/'+l.id,'View','text-button')}${!l.reserved&&!l.collected?link('/listings/'+l.id+'/edit','Edit','text-button'):''}${l.open?`<button class="text-button" data-action="close" data-id="${e(l.id)}">Close listing</button>`:''}</div></article>`).join('')||empty('No listings yet.','Publish surplus food when your kitchen is ready to share.')}`:''}<h2>Reservations & handovers</h2>${rs.sort((a,b)=>b.created-a.created).map(r=>pickup(r,hostel)).join('')||empty('No pickups yet.','Your reservations and completed handovers will appear here.')}<button class="text-button" data-action="logout">Sign out</button></div>`;
}
function pickup(r,hostel){return `<article class="pickup-row"><div><span class="badge">${r.status==='reserved'&&r.deadline<Date.now()?'Pickup window expired':e(r.status)}</span><h3>${e(r.meal)}</h3><p>${e(hostel?r.ngo:r.hostel)} · ${r.quantity} boxes reserved${r.status==='collected'?` · ${r.collected} collected`:''}</p><p>Expected arrival: ${date(r.eta)}</p><p>${e(hostel?r.contact:r.pickupContact)}</p><p>${e(r.pickupInstructions)}</p><small>Reference ${e(r.id)}</small></div><div class="pickup-action">${r.status==='reserved'?(hostel?`<form data-form="collect" data-id="${e(r.id)}">${field('code','NGO pickup code','','text','required pattern="[0-9]{6}" inputmode="numeric" autocomplete="off"')}${field('quantity','Actual boxes collected',r.quantity,'number',`required min="1" max="${r.quantity}" step="1"`)}<button class="button small">Confirm collection</button></form>`:`<button class="button small" data-action="code" data-id="${e(r.id)}">Show pickup code</button><button class="text-button" data-action="cancel-start" data-id="${e(r.id)}">Cancel reservation</button>`):''}</div></article>`;}
export function listingForm(s,id,privateInfo={}){if(!signed(s)||s.profile.role!=='hostel')return dashboard(s);const old=s.listings.find(l=>l.id===id);if(id&&(!old||old.hostelUid!==s.user.uid))return missing();const l={name:'',type:'Vegetarian',total:1,portions:1,area:'GLA University campus',prepared:Date.now(),deadline:Date.now()+7200000,...old,...privateInfo};return `<div class="page form-page">${link('/dashboard','← Your portal','back')}<h1>${old?'Edit food details.':'Share what’s available.'}</h1><p>${e(s.profile.name)} · Mathura</p><form class="listing-form" data-form="${old?'edit':'publish'}" data-id="${e(id||'')}"><div class="form-grid">${field('name','Meal name',l.name,'text','required maxlength="1500"')}<label>Food type<select name="type">${options(['Vegetarian','Vegan','Non-vegetarian'],l.type)}</select></label>${field('total','Total boxes',l.total,'number','required min="1" max="10000" step="1"')}${field('portions','Portions per box',l.portions,'number','required min="1" max="100" step="1"')}${field('area','General pickup area',l.area,'text','required')}${field('prepared','Prepared at',localInput(l.prepared),'datetime-local','required')}${field('deadline','Pickup deadline',localInput(l.deadline),'datetime-local','required')}</div><small>Input timezone: ${e(Intl.DateTimeFormat().resolvedOptions().timeZone)}. Published times show in IST.</small>${Object.entries({description:'Meal description and contents',ingredients:'Ingredients',allergens:'Known allergens',storage:'Storage and handling instructions',packaging:'Packaging and containers required',contact:'Private pickup contact',instructions:'Private gate / mess pickup instructions'}).map(([k,label])=>`<label>${label}<textarea name="${k}" required minlength="${k==='instructions'?10:k==='contact'?7:2}" maxlength="${k==='contact'?300:1500}">${e(l[k])}</textarea></label>`).join('')}<label class="check"><input type="checkbox" required>I have checked the food and these pickup details with the responsible kitchen staff.</label><button class="button">${old?'Save changes':'Publish food'}</button></form></div>`;}
export function admin(s){if(!s.admin)return `<div class="page"><h1>Administrator access required.</h1>${link('/login','Sign in with Google')}</div>`;return `<div class="page dashboard"><span class="eyebrow">ADMINISTRATION</span><h1>Keep the network trusted.</h1><p>Verify authorization directly with the hostel administration or the NGO before approving a request.</p><h2>Organization requests</h2>${s.requests.filter(p=>p.status==='pending').map(p=>`<article class="review-card"><span class="badge">${roleName(p.role)}</span><h3>${e(p.name)}</h3><p>${e(p.email)} · ${e(p.contact)}</p>${p.role==='hostel'?`<p><strong>GLA hostel:</strong> ${e(p.name)} · ${e(p.hostelGender||'')}</p>`:`<p><strong>Registration:</strong> ${e(p.registrationNumber||'Not provided')} · <strong>Website:</strong> ${e(p.website||'Not provided')} · <strong>Service area:</strong> ${e(p.serviceArea||'Not provided')}</p>`}<p>${e(p.evidence)}</p><form data-form="review" data-id="${e(p.id)}"><label>Verification decision<select name="status"><option value="approved">Approve organization</option><option value="rejected">Reject registration</option></select></label><label>Review note<textarea name="note" minlength="10" maxlength="1000" required placeholder="Record the verification performed or reason for rejection."></textarea></label><button class="button small">Save decision</button></form></article>`).join('')||empty('No pending requests.','New organization registrations will appear here.')}<h2>Food concerns</h2>${s.reports.filter(r=>r.status==='open').map(r=>`<article class="review-card"><h3>${e(r.listingId)}</h3><p>${e(r.reason)}</p><form data-form="resolveReport" data-id="${e(r.id)}"><label>Resolution<textarea name="note" required minlength="10" maxlength="1000"></textarea></label><label class="check"><input type="checkbox" name="close">Close the food listing</label><button class="button small">Resolve concern</button></form></article>`).join('')||'<p>No open concerns.</p>'}</div>`;}
export function missing(){return `<div class="page empty"><h1>This page isn’t available.</h1><p>The record may not exist or may still be loading.</p>${link('/explore','Browse available food')}</div>`;}
